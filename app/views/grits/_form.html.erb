<%= simple_form_for @grit, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :body %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                grits_path, :class => 'btn' %>
  </div>
<% end %>
